name: Main Workflow
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container: ubuntu:latest
    steps:
      - uses: actions/checkout@v1
      - run: apt-get update
      - run: apt-get install -y -q sudo wget git
      - run: mknod /dev/random c 1 8
      - run: ./travis/build.sh
