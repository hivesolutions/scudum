#!/bin/bash
# -*- coding: utf-8 -*-

DIR=$(dirname $(readlink -f $0))

set -e +h

if [ ! -e $DIR/extras.$1 ]; then
    echo "Command '$1' not found"
    exit 1
fi

$DIR/extras.$1 ${2} … ${N}

exit $?
