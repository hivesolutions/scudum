#!/bin/bash
# -*- coding: utf-8 -*-

DEV=${DEV-/dev/sda1}
TARGET=${TARGET-/scd}
LABEL=${LABEL-SSTORAGE}

echo "Going to start persistence for $DEV"

mkfs.ext4 $DEV
e2label $DEV "$LABEL"

mkdir -pv $TARGET
mount -v $DEV $TARGET
