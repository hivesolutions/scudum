#!/bin/bash
# -*- coding: utf-8 -*-

DEV=${DEV-/dev/sda1}
TARGET=${TARGET-/scd}

echo "Going to start persistence for $DEV"

mkfs.ext4 $DEV

cp -rp $TARGET $TARGET.back

mkdir -pv $TARGET
mount -v $DEV $TARGET

cp -rp $TARGET.back/* $TARGET
