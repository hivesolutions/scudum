#!/bin/bash
# -*- coding: utf-8 -*-

DEV=${DEV-/dev/sda1}
TARGET=${TARGET-/pst}
LABEL=${LABEL-PSTSCU}
FS=${FS-ext4}

echo "scu: creating persistence for $DEV using $FS"

mkfs.$FS $DEV
e2label $DEV "$LABEL"

mkdir -pv $TARGET
mount -v $DEV $TARGET

rm -rf $TARGET/lost+found
