#!/bin/bash
# -*- coding: utf-8 -*-

DEV=${DEV-/dev/sda1}
TARGET=${TARGET-/pst}
LABEL=${LABEL-PSTSCU}

echo "Going to start persistence for $DEV"

mkfs.ext4 $DEV
e2label $DEV "$LABEL"

mkdir -pv $TARGET
mount -v $DEV $TARGET
