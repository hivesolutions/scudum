#!/bin/bash
# -*- coding: utf-8 -*-

BUILD_NORMAL=${BUILD_NORMAL-1}
BUILD_BASIC=${BUILD_BASIC-1}

set -e +h

if [ "$BUILD_NORMAL" == "1" ]; then mkinitramfs; fi
if [ "$BUILD_BASIC" == "1" ]; then USE_FIRMWARE=0 mkinitramfs; fi
