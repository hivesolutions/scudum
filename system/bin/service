#!/bin/bash
# -*- coding: utf-8 -*-

set -e +h

if [ -z "$1" ]; then
    echo "No service name provided"
    exit 1
fi

if [ ! -e /etc/init.d/$1 ]; then
    echo "Service '$1' not found"
    exit 1
fi

/etc/init.d/$1 ${@:2}

exit $?
